# Use environment variable for nix-config path (configurable, defaults to standard location)
export NIX_CONFIG_PATH=${NIX_CONFIG_PATH:-$HOME/git/nix-config/main}
use flake ${NIX_CONFIG_PATH}/shells/ansible

# SOPS age key for secrets decryption
export SOPS_AGE_KEY_FILE=${SOPS_AGE_KEY_FILE:-$HOME/.config/sops/age/keys.txt}
