---
- name: Reload haproxy
  ansible.builtin.systemd:
    name: "{{ haproxy_service_name }}"
    state: reloaded

- name: Reload nginx
  ansible.builtin.systemd:
    name: "{{ haproxy_nginx_service_name }}"
    state: reloaded
